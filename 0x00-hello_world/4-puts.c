#include <stdio.h>
/**
 * main - A Program that uses the puts instead of printf
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle");
return (0);
}
